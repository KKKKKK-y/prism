# Notice

PRISM is distributed for academic review and non-commercial research evaluation. The public repository should not include generated datasets, trained checkpoints, model artifacts, deployment bundles, or result archives.

The final PRISM-Fusion planner uses a current-risk prior and uncertainty-aware predictive safe-risk refinement. The default planner setting is alpha-fusion with `alpha = 0.4`.
